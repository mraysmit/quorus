<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Timestamp for unique log filenames -->
    <timestamp key="testRunTime" datePattern="yyyyMMdd-HHmmss"/>
    
    <!-- Console appender with DEBUG level for tests -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File appender for test logs with MDC context and timestamp -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/test-logs/quorus-core-test-${testRunTime}.log</file>
        <append>false</append>
        <encoder>
            <!-- Include simulator and test context from MDC when available -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{simulator}/%X{testMethod}] %logger{40} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Rolling file appender (alternative - rotates by size) -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>target/test-logs/quorus-core-tests-rolling.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>target/test-logs/quorus-core-tests.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>5</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{simulator}/%X{testMethod}] %logger{40} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- SimulatorTestRunner at DEBUG level for test context -->
    <!-- Set to TRACE to also see OS/environment details (hostname, Java version, etc.) -->
    <logger name="SimulatorTestRunner" level="DEBUG"/>
    
    <!-- Quorus simulator packages at DEBUG level -->
    <logger name="dev.mars.quorus.simulator" level="DEBUG"/>
    
    <!-- Quorus packages at DEBUG level -->
    <logger name="dev.mars.quorus" level="DEBUG"/>
    
    <!-- Reduce noise from third-party libraries -->
    <logger name="org.apache" level="WARN"/>
    <logger name="io.vertx" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    
    <root level="INFO">
        <!-- Output to both console AND file -->
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
    
</configuration>
