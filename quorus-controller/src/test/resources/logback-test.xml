<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Console appender with DEBUG level for tests -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{requestId:-}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Consolidated file appender: all modules write to one file at project root.
         testRunTimestamp is passed as a system property from Maven surefire (parent pom.xml)
         so all modules share the same timestamp. prudent=true enables cross-JVM file locking. -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>../test-logs/quorus-test-${testRunTimestamp}.log</file>
        <append>true</append>
        <prudent>true</prudent>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{40} [%X{requestId:-}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Quorus packages at DEBUG level -->
    <logger name="dev.mars.quorus" level="DEBUG"/>
    
    <!-- Reduce noise from third-party libraries -->
    <logger name="org.apache" level="WARN"/>
    <logger name="io.vertx" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="io.grpc" level="WARN"/>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
    
</configuration>
