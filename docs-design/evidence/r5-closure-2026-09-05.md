# R5 handover closure evidence — 2026-09-05

R5 closes every remaining implementation item in the configuration-isolation handover.
RaftLog remains a separate sister repository at `../raftlog`, under the same parent as
Quorus. The earlier allegations of RaftLog 1.1 defects and an already-existing 1.2.0
release were unsubstantiated. The later 1.2.0 release from `1c5af80` is separately
verified and does not retroactively substantiate those claims.

## Implemented dispositions

| Item | Result | TDD evidence |
|---|---|---|
| Route-probe default ports | The probe and policy use `ServiceConnection.effectivePort`; portless endpoints use the documented protocol default | `r5-closure-route-trust-red.log`, then two tests green |
| Partial trust update | Omitted CA IDs, SSH pins, TLS pins and minimum TLS version retain their stored values | Same retained behavioral red and green |
| Controller/agent JSON drift | `ServiceConnectionJsonCodec` owns canonical redacted encoding and tolerant enum/default decoding | `r5-closure-codec-red.log`, then controller/agent regression green |
| Security-event reads | Tenant-scoped reads accept limit 1–1000 and an opaque cursor, return `nextCursor`, and avoid copying the complete metadata map | Corrected behavioral red in `r5-closure-events-behavioral-red.log`, then 14 tests green |
| Trust-manager lifecycle | Identical immutable policies reuse a manager through a 64-entry bounded cache; rotated policies get a distinct manager | `r5-closure-trust-cache-red.log`, then 46 tests green |
| Legacy credential replay | New URI user-info stays rejected; legacy protobuf commands are stripped and failed before state/snapshot persistence | `r5-closure-legacy-replay-red.log`, then 47 tests green |
| Configuration loaders | Resource loading, environment precedence and typed lookup mechanics share `LayeredProperties` | Refactor/characterization only; configuration regression green |
| Dead API surface | Unused convenience constructors removed; supported factory, NFS and HTTP server paths retained | Refactor/characterization only; focused regression green |
| Docker transfer script | Uses the current request fields and `jobId`; only credential-free HTTP fixtures are submitted | PowerShell parser green; live compose remains part of broader environment testing |

The authoritative security-event ledger is not automatically pruned. R5 makes reads
bounded; Phase 9 must still provide enterprise archive, legal hold and retention policy.
Governed HTTP clients remain per-transfer and are closed after use. R5 caches only the
immutable trust-manager construction result, preserving rotation isolation.

## Verification

Every Maven command used JDK 25 and redirected stderr through the evidence log with
`2>&1 | Tee-Object`. The combined focused regression completed at
`2026-09-05T19:45:33+08:00`: **254 tests passed**, zero failures, errors or skips,
across core, controller and agent. Its log SHA-256 is
`027DA7DBC90B68B48A9022D2AE19EB66526A71666BDA9C93B4F76317C27E0C4E`.

The first event invocation (`r5-closure-events-red.log`) had missing static test imports
and is not behavioral evidence. After correcting the test, the bounded-read assertion
failed against the old implementation and is the retained behavioral red. The first
legacy green attempt (`r5-closure-legacy-replay-green.log`) exposed compile mistakes in
the initial implementation and test; it is retained as a failed implementation run.
`r5-closure-legacy-replay-green-2.log` is the valid green result. Configuration-loader
and dead-constructor changes are verified refactors and are not represented as TDD.

R5 completion does not close the R1 container-recreation, selected production-filesystem
or machine power-loss gates. R6 final isolated source-tree acceptance is complete at
`b604505`; see [R6 evidence](r6-final-acceptance-2026-09-05.md).
